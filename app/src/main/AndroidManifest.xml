<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.meeting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".apis.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Auth_Reg.AuthWind"></activity>
        <activity android:name=".Meeting.StartMeetingActivity"></activity>
        <activity android:name=".Meeting.MeetingActivity"></activity>
        <activity android:name=".Meeting.AddUserToMeeting"></activity>
        <activity android:name=".Profile.UserProfile"></activity>
        <activity android:name=".Meeting.CreateMeeting"></activity>
        <activity android:name=".Auth_Reg.RegistrationActivity"></activity>
        <activity android:name=".Profile.addFiend"></activity>
        <activity android:name=".Meeting.MeetingSettings" android:windowSoftInputMode="adjustNothing"></activity>

        <service android:name=".Services.MessageService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>